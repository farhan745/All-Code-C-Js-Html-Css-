#include<bits/stdc++.h>
using namespace std;
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);//[Make more Fastest(cin,cout)]
#define endl '\n'
int  main(){

    optimize();
    
    string s="de",s1="AbCdE";//small latter paiority then capital latter
    if(s>s1) cout<<"Yes\n";
    else cout<<"No\n";

return 0;
}