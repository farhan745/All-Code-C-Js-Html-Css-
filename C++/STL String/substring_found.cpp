#include<bits/stdc++.h>
using namespace std;
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
int  main(){
optimize();
string s="shahriar";
if(s.find("ri") != -1)cout<<"Sub String Found";
else cout<<"Sub String Not Found";
return 0;
}